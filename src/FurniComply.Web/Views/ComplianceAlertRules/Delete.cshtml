@model FurniComply.Domain.Entities.ComplianceAlertRule

@{
    ViewData["Title"] = "Delete Alert Rule (Paused)";
}

<section class="hero">
    <div>
        <p class="eyebrow">Administration</p>
        <h1>Alert Rule Deletion Disabled</h1>
        <p class="lede">This module is temporarily hidden for your current workflow.</p>
    </div>
</section>

<div class="form-card">
    <div class="hero-actions module-header-actions">
        <a class="btn btn-outline-secondary" asp-action="Index"><i class="fa-solid fa-arrow-left"></i> Back to Alert Rules</a>
    </div>
</div>
